[package]
name = "mari-proto"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_bytes = "0.11"
ciborium = "0.2"
thiserror = "2.0"

[dev-dependencies]
serde_json = "1.0"
